export default defineAppConfig({
    ui: {
        colors: {
            primary: 'red', // Cambia 'blue' por el color que prefieras
            secondary: 'purple',
            neutral: 'zinc'
        }
    }
})
